generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
}

model Response {
  id               String   @id @default(cuid())
  createdAt        DateTime @default(now())
  data             Json
  completed        Boolean  @default(false)
  startYear        Int?
  affiliationYears Int?
  structureName    String?

  @@index([createdAt])
  @@index([completed])
  @@index([startYear])
}

model Admin {
  id       String @id @default(cuid())
  username String @unique
  password String
}
